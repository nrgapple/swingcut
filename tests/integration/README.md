# Integration tests

Integration tests must use synthetic media and mocked provider responses by default. Live Gemini or Photos tests require explicit opt-in and must not run in the standard test suite.
