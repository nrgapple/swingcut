# Private golden corpus

Store representative personal footage outside this repository. Never commit videos, exported Photos assets, proxies, faces, location metadata, or provider responses containing personal details.

When the evaluation harness exists, point it at an external directory through an explicit local configuration. Only schemas, anonymized annotations when safe, and aggregate metrics may be committed.
