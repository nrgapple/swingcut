# Native tests

The initial scaffold uses `make test-swift` for a build/version smoke test because this host currently exposes Swift through Command Line Tools without XCTest or Swift Testing modules. Add native unit tests here when PhotoKit behavior is implemented and validation selects a full Xcode test toolchain.
